#include "main.h"
/**
 * print_alphabet - print the alphabet in lowercase
 *
 * Return: 0 (success)
 */
void print_alphabet(void)
{
	char C;

	for (C = 'a'; C = 'z'; C++)
		_putchar (C);

	return (0);
}
